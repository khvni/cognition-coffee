import { ProsePullQuote } from "./ProsePullQuote"
import { ProseWaxFigure } from "./ProseWaxFigure"

/** Components made available to every MDX document via MDXProvider. */
export const mdxComponents = {
  ProsePullQuote,
  ProseWaxFigure,
}
